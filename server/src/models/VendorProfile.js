import { Vendor } from './Vendor.js';

/**
 * VendorProfile is the canonical role-specific profile model for Vendors.
 * It maps directly to the Vendor entity storing store, status, kyc, commission, and business references.
 */
export const VendorProfile = Vendor;
export default VendorProfile;
